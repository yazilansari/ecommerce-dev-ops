apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ecommerce  # Specify the namespace here
data:
  DB_HOST: "mysql-service" # Adjust if using an external DB (e.g., AWS RDS)
  DB_NAME: "ecommerce_db"
